package parsing_json;

//A Collection class for storing Elements
/*
The Element collection should extend ArrayList and implement 3 methods.

findByAtomicNumber will take an argument int atomicNumber and return the Element with number atomicNumber

findByName will take an argument String name and return the Element with name name

where will take arguments String field and Object value. If Element has an attribute with the name field then the method
will go through each Element in the collection and return a new ElementCollection with all Elements who's field matches
 the Object value.
If no attribute is
 called field then the method should simply return an empty ElementCollection.
 */

public class ElementCollection {

    public Element findByAtomicNumber(int atomic_number) {

        return null;
    }

    public Element findByName(String name) {

        return null;
    }

    public ElementCollection where(String fieldName, Object value) {

        return null;
    }
}
